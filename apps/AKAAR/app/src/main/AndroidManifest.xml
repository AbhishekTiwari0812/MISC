<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhishektiwari.akaar" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage"
            android:label="Home"></activity>
        <activity android:name=".TodayStat"
            android:label="Performance Today"></activity>
        <activity android:name=".CompleteStat"
            android:label="OverAll statistics"></activity>
        <activity android:name=".StudentList"
            android:label="Student List"></activity>
    </application>

</manifest>
